import 'package:flutter/material.dart';

class AppColors {
  // Main Brand Colors
  static const Color primaryNavy = Color(0xFF001F3F); 
  static const Color accentGold = Color(0xFFC6A664); 
  static const Color accentAmber = Color(0xFFC6A664); 

  // Neutral Colors
  static const Color bgLight = Color(0xFFF8FAFC);
  static const Color white = Colors.white;
  static const Color textPrimary = Color(0xFF1F2937);
  static const Color textSecondary = Color(0xFF6B7280);
  
  // Minimalist Status (Using brand shades)
  static const Color success = Color(0xFF001F3F); // Use Navy for success (premium feel)
  static const Color error = Color(0xFFC6A664);   // Use Gold for attention/error
  static const Color warning = Color(0xFFC6A664);
}
